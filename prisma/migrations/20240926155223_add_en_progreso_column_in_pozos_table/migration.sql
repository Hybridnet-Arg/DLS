BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[pozos] ADD [en_progreso] BIT NOT NULL CONSTRAINT [pozos_en_progreso_df] DEFAULT 0;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
