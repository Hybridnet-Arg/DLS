BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[tubulares_documentos] ADD [fecha_expiracion] DATETIME2;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
