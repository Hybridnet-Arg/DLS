BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[locaciones] ADD [fecha_fin] DATETIME,
[fecha_inicio] DATETIME;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
