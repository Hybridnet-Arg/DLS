BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[locaciones] ADD [eliminado] BIT NOT NULL CONSTRAINT [locaciones_eliminado_df] DEFAULT 0;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
